<% if obj.errors.any? %>
  <h2><%= obj.errors.count %> errors prohibited this object from being saved.</h2>
  </br>
  <ul>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>